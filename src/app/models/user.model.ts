export interface User {
  id: string;
  fullname: string;
  email: string;
  point: number;
  phone: string;
  createdAt: string; 
}

export interface Auth {
  id: string;
  fullname: string;
  createdAt: string; 
}